package com.parsfilo.contentapp.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// ── Neutrals ────────────────────────────────────────────────────────────────
val app_neutral_white       = Color(0xFFFAFAFA)
val app_neutral_black       = Color(0xFF0A0A0F)
val app_neutral_gray        = Color(0xFF7A7A8A)
val app_neutral_red         = Color(0xFFE53935)
val app_transparent         = Color(0x00000000)

// ── Semantic ─────────────────────────────────────────────────────────────────
val app_success             = Color(0xFF2E7D52)
val app_error_soft          = Color(0xFFE57373)
val app_error_dark          = Color(0xFF8B0000)

// ── Shadows / overlays ───────────────────────────────────────────────────────
val app_shadow_strong       = Color(0x33000000)
val app_shadow_soft         = Color(0x1A000000)

// ── Text ─────────────────────────────────────────────────────────────────────
val app_text_almost_black   = Color(0xFF12121A)
val app_text_light          = Color(0xFFE2C96A)   // altın/krem — Arabic metin üzeri

// ── FAB / Nav ────────────────────────────────────────────────────────────────
val app_curved_fab_default  = Color(0xFF7C5CBF)

// ── Premium surface overlays (light mode kart tint'leri) ─────────────────────
val app_surface_tint_warm   = Color(0xFFF5EFE8)   // krem-altın
val app_surface_tint_cool   = Color(0xFFEEF0F8)   // gri-mavi
val app_surface_tint_green  = Color(0xFFEBF3ED)   // mint-yeşil
val app_surface_tint_rose   = Color(0xFFF5ECF0)   // gül
